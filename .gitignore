# ---------------------------------------------------------------------------
# Dependencies
# ---------------------------------------------------------------------------
node_modules
.pnp
.pnp.js

# ---------------------------------------------------------------------------
# Environment & secrets  (NEVER commit real credentials)
# ---------------------------------------------------------------------------
.env
.env.*
# ...but keep the templates so others know which vars to set:
!.env.example
!*/.env.example
!**/.env.example
*.pem
*.key
*.p12
*.pfx
*.cert
secrets/
.secret*
credentials.json
service-account*.json

# ---------------------------------------------------------------------------
# Build output
# ---------------------------------------------------------------------------
.next
out
dist
build
.turbo
*.tsbuildinfo

# ---------------------------------------------------------------------------
# Test / coverage
# ---------------------------------------------------------------------------
coverage
playwright-report
test-results
.cache

# ---------------------------------------------------------------------------
# Logs
# ---------------------------------------------------------------------------
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# ---------------------------------------------------------------------------
# Hosting / tooling
# ---------------------------------------------------------------------------
.vercel
.aider*

# ---------------------------------------------------------------------------
# Editors / OS
# ---------------------------------------------------------------------------
.idea
.vscode
*.swp
.DS_Store
Thumbs.db

# ---------------------------------------------------------------------------
# S3 / CloudFront infrastructure
# ---------------------------------------------------------------------------
Plans/s3-cdn/.keys/
Plans/s3-cdn/.state

# ---------------------------------------------------------------------------
# AWS automation secret inputs (NEVER commit). config.env + lib.sh + *.sh are
# safe to commit; these hold real secrets / private keys and must not be.
# ---------------------------------------------------------------------------
Plans/aws-automation/secrets.env
Plans/aws-automation/amplify-env.env
Plans/aws-automation/*.pem
Plans/aws-automation/*-new.json
Plans/aws-automation/.state
Plans/inference-aws/persona-output/**/*.png
Plans/inference-aws/persona-output-v2/**/*.png
