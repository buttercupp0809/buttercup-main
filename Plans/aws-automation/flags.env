# Minimal env file: only the two feature flags to push to prod backend.
# Contains NO secrets. Applied via 13-set-env-vars.sh so it does not touch the
# empty-valued API-key entries in secrets.env. Safe to delete after applying.
IMG_FACEDETAILER=1
WAN_INTERPOLATION=0
